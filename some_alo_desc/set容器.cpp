
// 本节介绍set容器

/*
    insert();

    find(val);

    erase();

    size();

    clear();


*/





/*

    set, map, multimap, 基于平衡二叉树，动态维护有序序列
        size()
        empty()
        clear()



    set/multiset
        insert()
        find()
        count()
        erase()
            (1)输入一个数x，删除所有x
            (2)输入一个迭代器，删除这个迭代器
        lower_bound()/upper_bound()


    map/multimap
        insert() 插入的数是一个pair
        erase() 输入的参数是pair或者迭代器
        find()




    unordered_set, unordered_map, unordered_multiset, unordered_multimap
        和上面类似，增删改查的时间复杂度是o(1)
        不支持 lower_bound() 和 upper_bound()



    bitset
        count() 返回多少个1
        any() 判断是否至少有一个1
        none() 判断是否全是0

        set() 把所有位置成1
        set(k, v) 把第K位变成v

        reset()
        flip()
        flip(k) 第k位取反
        

*/
