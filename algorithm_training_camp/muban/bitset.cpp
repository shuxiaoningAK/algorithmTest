/* bitset

    bitset是一个多位二进制数，如同状态压缩的二进制数
    count();
    any();
    none();
    set();
    set(k);
    set(k, val);
    reset();
    reset(k);
    flip();
    flip(k);
    size();
    to_ulong();
    to_string();

*/
