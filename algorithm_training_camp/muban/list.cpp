/* list

    merge(b);
    remove(val);
    splice(pos, b);
    reserve();
    sort();
    unique();  将连续的相同元素压缩为单个元素。不连续的相同元素无法压缩。因此一般先排序后去重

    push_front(x)  push_back(x);
    pop_front() pop_back();
    front() back();
    insert();
    erase();
    clear();


*/
