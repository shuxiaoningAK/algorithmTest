#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b, c, d;
    int sum1, sum2;

    cin >> a >> b >> c >> d;

    while (a != 0) {
        if (a % 10 == b) sum1 += b * 10;
    }

}